day_of_week = input("What dat of the week is it today?")
	
if day_of_week == "Monday":
	print("Have a great start to your week") // with indentation inside condition
print("This always run.") // without indentation not inside condition

	
if (day_of_week == "monday"):
	print("Have a great start to your week")
elif day_of_week == "tuesday":
	print("it's tuesday.")
else:
	print("Full speed ahead!")
print("This always run.")
