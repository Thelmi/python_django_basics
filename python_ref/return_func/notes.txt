
def add(x, y):
	return x + y

result = add(4 , 10);
print(result)


def divide(dividend, divisor=0):
	if divisor != 0:
		return dividend / divisor // different type, umm interesting 
	else:
		return "You fool!" // different type, umm interesting, not very recomended
		
result = divide(15, 2) * 5
print(result)